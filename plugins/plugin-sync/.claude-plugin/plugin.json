{
  "name": "plugin-sync",
  "description": "Repairs the ways an installed plugin set drifts from what its marketplaces offer: a plugin its marketplace no longer lists, one enabled but never installed, one installed twice from two marketplaces. Reports at session start, fixes on request, and leaves a healthy install alone — shipping versions is auto-update's job.",
  "version": "0.1.0",
  "author": {
    "name": "Dan Fox"
  },
  "keywords": [
    "mechanism"
  ]
}
